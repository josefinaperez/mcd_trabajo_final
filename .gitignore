# macOS
.DS_Store
*.DS_Store

# RStudio / R
.Rproj.user
.Rhistory
.RData
.Ruserdata

# R notebook knit / render artifacts
r/notebooks/*.html
r/notebooks/*_files/
r/notebooks/*_cache/

# Logs (los pipelines de R escriben *.log)
*.log

# Data (inputs crudos + outputs generados; solo se trackea data/ocurrences/raw/*.csv — ver CLAUDE.md)
data/
*.csv

# Carpeta de notas locales del usuario (no se comittea)
docs/
